{
  "name": "penpot-to-flutter",
  "private": true,
  "version": "0.0.33",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc && vite build",
    "test": "rm -rf .test-build && tsc -p tsconfig.test.json && node --test .test-build/test/conversion.test.js"
  },
  "dependencies": {
    "@penpot/plugin-types": "1.5.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.5",
    "vite-live-preview": "^0.3.2"
  }
}
